<%= semantic_form_for @user, :class => 'form-stacked' do |f| %>
  <div class="field">
    <b>Selecteer Foto:</b> <%= f.file_field :picture %>
  </div>	
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>